# Shared CI building blocks -- fragments and templates consumed by every
# other file under .gitlab/ci/. Included FIRST in the root .gitlab-ci.yml's
# `include:` list: every other file references `.fragments` and the
# templates below by name, so they must already be in scope.
#
# Why this file exists: YAML anchors (`&name` / `*name` / `<<:`) do not
# cross `include:` boundaries -- each included file is parsed as its own
# YAML document, so an anchor defined in lint.yml is invisible to test.yml
# even though both end up in the same merged pipeline. That is why, before
# this file, `.install-uv` existed as four separate copies with three
# different bodies: nobody could point at a single shared definition. Both
# `extends:` and the `!reference` tag are GitLab CI's own post-processing
# step, applied after every included file has already been parsed and
# merged into one job namespace -- so, unlike anchors, they do cross
# `include:`. `!reference [job, key, ...]` splices in another job's key
# verbatim; `extends:` merges one job's keys onto another's, with a
# `variables:`-style hash key deep-merged and everything else (`script`,
# `before_script`, `cache`, `rules`, ...) replaced wholesale by the more
# specific side, not appended. That "replace, not append" rule is exactly
# why composing script fragments here uses `!reference` -- extending a
# template and then trying to add more `before_script` lines on top would
# silently discard the template's lines instead of running after them.
#
# verify vs test: the two words that name the first two pipeline phases
# read as synonyms, so the distinction is written down once, here, rather
# than left to be inferred per job. `verify` asks whether something matches
# what it is required to match -- a licence against policy, one VM
# implementation's output against the other's. `test` asks whether
# something does what it should when actually executed. This is not a
# static-vs-dynamic split (`verify:parity`, once it exists, runs the VM);
# it is consistency between artefacts versus correctness of one artefact.
# The old `lint` stage held four unrelated concerns because nothing forced
# a new job into naming which question it was answering -- an unwritten
# boundary is how that stage decayed, and restating it in prose here is
# what stops the replacement from decaying the same way.
#
# The `--only` mechanism (`.rust-tools` below, `cargo-tools-only`):
# `scripts/install-cargo-tools.sh --only <name>[,<name>...]` installs a
# named subset of the pinned tools in scripts/cargo-tools.lock instead of
# the full set `make deps` installs. It fails, listing every unmatched
# name, if any requested tool isn't found in the lock file -- so a typo in
# a job's `CARGO_TOOLS` list cannot silently install nothing and pass. This
# is what lets a job that needs only e.g. `git-cliff` or `mdbook` skip
# installing (and caching) the rest of the toolchain.
#
# Job timeouts (audit finding CI-20): every job in verify.yml, test.yml,
# docs.yml and hardware.yml, and all five jobs in release.yml, carries an
# explicit `timeout:`. Without one the project default applies uniformly to
# a 12-second job and a 10-minute one, so a hang is only caught an hour
# later.
#
# These are sized for a COLD CACHE, which on this runner fleet is a normal
# outcome rather than an edge case. The runners have no shared cache server
# -- a job's log reads "No URL provided, cache will not be downloaded from
# shared cache server. Instead a local version of cache will be extracted."
# Caches are therefore local to whichever runner wrote them, and a job only
# gets a hit when it happens to land on that same machine. Every duration
# this repo has measured is a warm-or-partly-warm sample; the cold path is
# a different regime entirely, not a tail of the same distribution.
#
# That distinction is not theoretical. An earlier revision of this block
# sized every timeout at 3x the slowest observed run, which put
# test:substrate at 10m. It then missed its cache, rebuilt the polkadot-sdk
# git dependency from scratch, and was killed at 600s by that very timeout.
# The job was not hung; it was doing the work a cold run requires.
#
# So the rule here is: a timeout exists to kill a job that will never
# finish, not to police one that is merely slow. A genuinely hung job runs
# forever, so generous limits catch it just as reliably as tight ones and
# cost nothing when things are healthy. Compile-heavy jobs get room for a
# full cold build of what they touch; test:substrate gets the most
# because a cold run clones the polkadot-sdk fork and compiles the
# fixture's 386-package FRAME dependency tree (frame-support,
# frame-system, sp-runtime, plus sp-core and sp-io for the tests) from
# scratch. There is no node, no wasm runtime and no consensus code in
# that build -- it is a pallet fixture, not a chain.
#
# The three hardware:* jobs still have no run data -- they run only on
# protected refs and have never executed. Their values are deliberate
# guesses with cold-start headroom for what is specific to each (driver
# and kernel warmup for cuda, a real network round-trip to a D-Wave QPU
# for qpu, local Apple Silicon execution for metal) plus, since QUI-1191,
# a cold cargo fetch and a full xqvm + xqasm + pyo3 compile that all three
# now carry: scripts/run-hardware-tests.sh rebuilds xqffi's cdylib on
# every run, which is the whole point of that change. That put cuda and
# qpu at 45m and metal at 30m, the lower value reflecting that metal alone
# has neither queueing nor driver warmup underneath the compile.
#
# The compile lands on top of the cold-cache regime described above rather
# than replacing it, and hardware:qpu is the worst case in the fleet: its
# `[linux, x86_64]` tags match three runners, so with no shared cache
# server its cargo cache hits roughly one run in three. Revisit all three
# once real timings exist.
#
# Scope note (post-QUI-1002): `release.yml` used to keep its own three
# anchors (`.setup-python-publish`, `.on-release-tag`, `.on-pr-or-push`)
# rather than moving onto these templates. That fifth `uv` variant is now
# folded in as the `python-publish` entry in `.fragments` below -- read it
# for the PATH-ordering rationale. `.on-pr-or-push` was deleted along with
# its two dry-run consumers (`release:dry-run:crates`,
# `release:dry-run:pypi`), which QUI-1002 replaced with the single
# `release:validate` job described in the "Dependency gating" section
# below. `.on-release-tag` stays local to `release.yml` deliberately: it
# has exactly one consuming file, and an anchor that only ever needs to
# work within one document gets nothing from living here instead.

# Hidden job holding named script sequences, composed into the templates
# below (and, where a job's shape doesn't match any template, referenced
# directly by that job) via `!reference`. Not a real job -- GitLab skips
# any top-level key starting with `.` when building the pipeline.
.fragments:
    cargo-path:
        - export PATH="${CARGO_HOME}/bin:${PATH}"
    # Path plus the full pinned dev-tool set (scripts/cargo-tools.lock).
    cargo-tools:
        - !reference [.fragments, cargo-path]
        - make deps
    # Path plus a named subset of the pinned tools -- CARGO_TOOLS is a
    # comma-separated list the consuming job must set, e.g.
    # `CARGO_TOOLS: mdbook,mdbook-mermaid`.
    cargo-tools-only:
        - !reference [.fragments, cargo-path]
        - bash scripts/install-cargo-tools.sh --only "${CARGO_TOOLS}"
    uv:
        # UV_VERSION lives in the Makefile beside RUFF_VERSION, and is
        # grepped out here rather than pinned a second time -- the same
        # single-source-of-truth idiom .githooks/pre-commit already uses
        # for RUFF_VERSION.
        #
        # The two pins are not equally binding, and the difference
        # matters. RUFF_VERSION is INSTALLED by both sides: local dev
        # gets it through `uvx ruff@$(RUFF_VERSION)` in lint-py /
        # fmt-check-py and through .githooks/pre-commit, so the versions
        # cannot diverge. UV_VERSION is installed by CI only -- a
        # contributor's `uv` is whatever they put on their PATH, and
        # nothing can install uv through uv. Local dev CHECKS the pin
        # instead: `make check-uv-lock` warns (does not fail) when the
        # local uv differs, because since QUI-1190 `uv lock --check`
        # gates both `make preflight-py` and verify:python, and a
        # resolver difference between two uv versions otherwise presents
        # as a stale lockfile rather than as what it is.
        - UV_VERSION="$(sed -n 's/^UV_VERSION[[:space:]]*:=[[:space:]]*//p' "${CI_PROJECT_DIR}/Makefile" | head -n1)"
        - '[ -n "${UV_VERSION}" ] || { echo "uv: could not read UV_VERSION from Makefile" >&2; exit 1; }'
        - curl -LsSf --retry 3 --retry-all-errors "https://astral.sh/uv/${UV_VERSION}/install.sh" | sh
        - export PATH="${HOME}/.local/bin:${PATH}"
        - export UV_CACHE_DIR="${CI_PROJECT_DIR}/.uv-cache"
    # uv on PATH plus a full workspace sync -- installs xqvm_py/xqcp/xqsa/
    # xqffi/xquad editable into .venv/ and builds xqffi's cdylib through
    # maturin (xqffi is a uv workspace member).
    uv-sync:
        - !reference [.fragments, uv]
        - uv sync
    # uv + maturin + twine + cargo-zigbuild for the Python publishing
    # path (release:validate, release:pypi). Stays on rust:latest (the
    # workspace default) so cargo is available for maturin's cdylib
    # build; uv provides the wheel builder for the pure-Python peers.
    python-publish:
        # Must come first: this is what puts ${CARGO_HOME}/bin on PATH,
        # which is what makes the `command -v cargo-zigbuild`
        # short-circuit in scripts/install-cargo-tools.sh reachable.
        # Without it the cached cargo-zigbuild binary is invisible and
        # the job reinstalls on every run -- this was the live defect
        # the ticket exists to fix.
        - !reference [.fragments, cargo-path]
        - apt-get update && apt-get install -y --no-install-recommends python3 python3-pip python3-venv curl ca-certificates
        # Reusing the uv fragment also sets UV_CACHE_DIR, which the
        # release jobs do not cache today. Left uncached rather than
        # inventing a cache key here -- a known follow-up, not a
        # regression.
        - !reference [.fragments, uv]
        - uv --version
        # ziglang rides along in maturin's uv-tool venv (--with) so the
        # `python3` first on PATH (the venv's) can `python3 -m ziglang`
        # -- that is how cargo-zigbuild locates zig for the aarch64
        # build. A plain `pip3 install ziglang` lands in the system
        # python, which is NOT the python3 on PATH here, so
        # cargo-zigbuild can't find it.
        - uv tool install maturin --with-requirements /dev/null --with ziglang
        # twine >=6.1 added preliminary OIDC trusted-publishing support;
        # earlier versions fall back to an interactive prompt when no
        # token env is present, which hangs the CI.
        - uv tool install 'twine>=6.1'
        - export PATH="${HOME}/.local/share/uv/tools/maturin/bin:${HOME}/.local/share/uv/tools/twine/bin:${PATH}"
        - maturin --version
        - twine --version
        # cargo-zigbuild drives the aarch64 cross-build (zig as the
        # linker; zig itself comes from the ziglang package installed
        # above). Pinned in scripts/cargo-tools.lock and installed via
        # the pinned install-cargo-tools.sh (binstall, cached) instead
        # of a bare `cargo install` that recompiles from source every
        # run.
        - bash scripts/install-cargo-tools.sh --only cargo-zigbuild
        - rustup target add aarch64-unknown-linux-gnu
    apk:
        - apk add --no-cache ${APK_PACKAGES}

# ---------------------------------------------------------------------------
# Templates
# ---------------------------------------------------------------------------

# Full Rust toolchain (`make deps`). Cache is left at the root `default:`
# two-entry cargo cache (registry/git/target keyed on Cargo.lock, .cargo/
# bin/ keyed on scripts/cargo-tools.lock) -- consumers here compile the
# workspace, so both entries are live.
#
# CACHE WRITERS: this template's consumers (verify:rust, test:rust,
# test:wasm) are the pipeline's only pushers for BOTH root default entries.
# `make deps` always installs the FULL scripts/cargo-tools.lock set, not a
# subset, so the `.cargo/bin/` entry is fully repopulated here even though
# `.rust-tools` below only ever installs a named subset of it.
#
# The guarantee rests on verify:rust and test:rust alone. Those two are
# unconditional -- no `rules:` skips them on any pipeline shape -- so both
# root entries have a pusher on every pipeline. test:wasm is path-gated
# (see test.yml's "Path gating" section): when it is created it writes the
# same two entries, but it is a bonus writer and must never be cited as the
# reason an entry stays populated. Do not make verify:rust or test:rust
# `policy: pull`; that would leave both root entries with no guaranteed
# pusher at all.
.rust:
    before_script:
        - !reference [.fragments, cargo-tools]

# Rust toolchain, but only the named subset of cargo-tools.lock's tools a
# job actually runs (see the `--only` note above). Cache narrows to just
# `.cargo/bin/` -- these jobs never touch `target/` or `.cargo/registry/`,
# so restoring/saving the full cache would be pure waste.
#
# `policy: pull`: every tool a `.rust-tools` consumer installs is a proper
# subset of what `.rust`'s `make deps` already installs above, and two of
# `.rust`'s three consumers -- verify:rust and test:rust -- run
# unconditionally on every pipeline (test:wasm is path-gated; see the writer
# note on `.rust`). That makes both current consumers of THIS template
# (verify:policy: taplo,cargo-deny,git-cliff; docs:build:
# mdbook,mdbook-mermaid) pure readers: whatever they need here is already
# guaranteed present, or about to be re-pushed, by verify:rust / test:rust.
# Pulling instead of pushing loses nothing.
#
# The policy sits on the template rather than per-job on purpose: "install a
# subset already covered by `.rust`'s full install" is the whole reason this
# template exists, so a future job that extends it for the same reason
# inherits the correct (pull) policy for free. If a future `.rust-tools`
# consumer ever needs a tool `.rust`'s `make deps` does NOT already install,
# `.rust-tools` stops being the right fit for it and this policy needs
# revisiting -- do not add such a job here without also checking this note.
.rust-tools:
    before_script:
        - !reference [.fragments, cargo-tools-only]
    cache:
        - key:
              files:
                  - scripts/cargo-tools.lock
          paths:
              - .cargo/bin/
          policy: pull

# uv plus a full workspace sync. Consumers need the built xqffi extension,
# not just a linter -- e.g. running the Python test suite or shelling out
# to `python -m xqvm_py` for cross-implementation parity.
#
# `uv sync` builds xqffi's cdylib through maturin (xqffi is a uv workspace
# member), so every consumer here also pays a Rust compile -- the cargo
# cache entries below are just as live as they are for `.rust`. Before
# QUI-1001 Phase C2, this template's cache *replaced* the default cargo
# cache with only the uv entry, so that compile ran from scratch on every
# pipeline; it is now the default two entries plus a third for uv, so a
# consumer job restores all three.
#
# The uv entry deliberately carries no `fallback_keys:`. A prior
# `fallback_keys: [main-uv]` here could never match -- the root cache's
# keys are hash-only (see .gitlab-ci.yml's `default:` comment), so no
# `main-*` key has ever existed to fall back to -- and its comment claimed
# to share a pattern the root config deliberately omits as non-functional.
# Dead config plus a misleading comment (audit finding CI-10).
#
# CACHE WRITERS: all three current consumers (verify:parity, test:python,
# test:quip) genuinely write here, so none of them becomes `policy: pull`.
# verify:parity and test:python both pull in the full `deps-py` sequence
# (uv sync + an explicit `maturin develop`) via their Makefile
# prerequisites, which overlaps between the two but is not a
# no-op for either -- each performs the real resolve/compile independently,
# and either alone would be a sufficient pusher if the other job were ever
# removed. test:quip additionally does its own `uv run --extra quip` sync,
# which pulls in the quip_signer wheel neither of the other two installs --
# it is the sole writer for that part of the cache's contents. Leave all
# three at the default pull-push policy.
.python:
    before_script:
        - !reference [.fragments, uv-sync]
    cache:
        - key:
              files:
                  - Cargo.lock
          paths:
              - .cargo/registry/
              - .cargo/git/
              - target/
        - key:
              files:
                  - scripts/cargo-tools.lock
          paths:
              - .cargo/bin/
        - key:
              files:
                  - uv.lock
                  - pyproject.toml
          paths:
              - .uv-cache/
              - .venv/

# uv on PATH, workspace NOT synced. For jobs that need only a Python
# interpreter plus a small, separately-pinned set of packages (`uvx
# <tool>@<version>`, or `uv run --no-project --with <dep>`), not the full
# maturin-built xqffi cdylib. The `uvx` key prefix keeps this cache from
# colliding with `.python`'s archive under the same uv.lock/pyproject.toml
# hash -- a synced payload and a sync-less payload are not interchangeable,
# so sharing one key would mean whichever job runs first "wins" and the
# other always cold-starts.
#
# CACHE WRITERS: both current consumers (verify:python, docs:generated)
# write DIFFERENT content into this same `.uv-cache/` bucket and neither is
# a subset of the other, so both must stay pushers. verify:python's `uvx
# ruff@...` (via lint-py / fmt-check-py) caches ruff's own isolated tool
# venv; docs:generated's `uv run --no-project --isolated --with
# pyyaml==...` (the Makefile's DOCSGEN -- `--isolated` only isolates the
# venv from project/workspace discovery, it does not bypass the package
# cache) caches PyYAML instead. Making either one `policy: pull` would mean
# that package is never cached at all, not just cached less often.
.python-tools:
    before_script:
        - !reference [.fragments, uv]
    cache:
        - key:
              prefix: uvx
              files:
                  - uv.lock
                  - pyproject.toml
          paths:
              - .uv-cache/

# No cargo, no uv -- just apk and a shell. `cache: []` for jobs that
# compile nothing and only run a bash script; downloading the (irrelevant)
# default cargo cache into an alpine container is pure waste.
.alpine:
    image: alpine:3
    cache: []

# Hardware-backed solver jobs (hardware:cuda / hardware:qpu /
# hardware:metal). Shares the protected-ref rule common to all three --
# see hardware.yml's header for why -- and gets uv onto PATH. It stops at
# the plain `uv` fragment because no job installs anything in its
# before_script: each `script:` is a single `make -k test-<kind>`, and
# scripts/run-hardware-tests.sh behind it owns the whole sequence (sync the
# solver extra, rebuild xqffi through maturin, probe the device, run
# pytest).
#
# Before QUI-1001 Phase C2, none of the three jobs had a cache entry at
# all -- they set UV_CACHE_DIR but nothing covered that path, so each
# pipeline wrote a uv cache and immediately threw it away, while still
# paying to restore/save the (irrelevant) default cargo cache.
#
# The cache key PREFIX is `hw-$CI_JOB_NAME`, not a shared key:
# hardware:metal runs on a macOS runner while hardware:cuda /
# hardware:qpu run on Linux, and BOTH entries below hold host-specific
# build output -- `.uv-cache/` holds built wheels including the
# maturin-built xqffi cdylib, and `target/` holds Rust object files for
# whichever host compiled them. A single shared key would let a Darwin
# archive poison the Linux jobs (or vice versa). Per-job-name prefixing
# keeps each job's cache in its own bucket even though all three hash the
# same uv.lock/pyproject.toml and the same Cargo.lock.
#
# The cargo entry exists because `cache:` REPLACES the root `default:`
# cache wholesale rather than adding to it: until QUI-1191 these three
# jobs restored no `target/`, no `.cargo/registry/` and no `.cargo/git/`
# at all. That cost nothing while they only ran pytest out of a prebuilt
# uv cache. It became the dominant cost the moment
# scripts/run-hardware-tests.sh started rebuilding xqffi's cdylib through
# maturin, which is a real compile of xqvm + xqasm + pyo3 on every run.
#
# Note this key deliberately does NOT reuse the root default's hash-only
# Cargo.lock key. That one is project-wide and shared with verify:rust /
# test:rust, and reusing it here is precisely the Darwin-poisons-Linux
# hazard above. The price is that these three never inherit main's warm
# cargo cache and cold-start on their first pipeline after any Cargo.lock
# bump -- accepted, because a wrong-host `target/` is a corrupted build,
# not a slow one.
#
# Expect this entry to MISS often, and size the timeouts for that rather
# than for the hit. Per the "Job timeouts" section above the fleet has no
# shared cache server, so an entry is local to whichever runner wrote it
# and only hits when the job lands on that same machine again. How often
# that happens is a property of each job's tags: hardware:metal has one
# macOS box and so is reliably warm after its first run; hardware:cuda's
# `cuda` tag matches two runners; hardware:qpu's bare `[linux, x86_64]`
# matches three, so roughly two runs in three pay the full cold build AND
# upload a fresh `target/` archive the next run elsewhere will not read.
#
# That is a weak cache, not a useless one -- the hits are real and cost
# nothing to collect -- but it is why the compile has to fit inside each
# job's timeout unaided. Do not let this entry's existence talk a future
# reader into tightening those limits.
#
# On the uv side only `.uv-cache/` is cached, deliberately never `.venv/`:
# each job syncs a *different* extra, so a cached venv from one job's sync
# would be the wrong environment for another's -- don't "simplify" this
# back to matching `.python`'s two paths.
#
# CACHE WRITERS: the `hw-$CI_JOB_NAME` prefix means hardware:cuda,
# hardware:qpu and hardware:metal each own an entirely separate cache slot
# -- there is no other job sharing any one of these keys, so each job is
# necessarily the sole writer of BOTH of its own entries and neither can
# become `policy: pull` without that job's cache going permanently
# unpopulated.
.hardware:
    rules:
        - if: $CI_COMMIT_REF_PROTECTED == "true"
    before_script:
        - !reference [.fragments, uv]
    cache:
        - key:
              prefix: "hw-$CI_JOB_NAME"
              files:
                  - uv.lock
                  - pyproject.toml
          paths:
              - .uv-cache/
        - key:
              prefix: "hw-$CI_JOB_NAME"
              files:
                  - Cargo.lock
          paths:
              - .cargo/registry/
              - .cargo/git/
              - target/

# ---------------------------------------------------------------------------
# Dependency gating
# ---------------------------------------------------------------------------

# The five phases are a deliberate gating topology, not five stages that
# merely happen to run one after another:
#
#   verify:*, test:*  -- all ten jobs carry `needs: []` and start at t=0.
#                         verify and test are peers, not a chain -- test
#                         does NOT wait for verify to pass first. Eight of
#                         the ten are created on every pipeline; test:wasm
#                         and test:substrate are path-gated and may be
#                         absent -- see test.yml's "Path gating" section.
#   hardware:*        -- each of the three jobs needs every verify:* and
#                         test:* job. Real hardware -- a GPU, a metered
#                         D-Wave QPU token, a macOS box -- is scarce and/or
#                         billed, so nothing touches it before the code is
#                         known to compile and pass its own test suite.
#   docs:*, release:validate -- gated behind verify + test + hardware.
#                         Docs and release:validate start together, rather
#                         than docs waiting on release or vice versa; the
#                         tag-only publish chain in release.yml
#                         (release:crates -> release:pypi ->
#                         release:notes) hangs its own `needs:` off
#                         release:validate rather than off this gate
#                         directly, so it is gated transitively instead of
#                         through the stage barrier.
#
# Gating docs behind hardware means an offline GPU runner or an expired
# D-Wave token stalls the documentation site, even though nothing in the
# docs content depends on hardware passing. That is an accepted tradeoff,
# not an oversight: hardware correctness gates publishing because we would
# rather stall the docs than publish a book describing an opcode or solver
# behaviour the hardware suite just proved broken. Since release:validate
# now carries this same gate on every ref including tags, the cost is no
# longer confined to docs: on a tag the publish chain waits on it too, so
# an expired D-Wave token stalls a release, not just the documentation
# site -- and test:substrate's cold-cache path is the largest single
# component of that wait.
#
# hardware:* jobs are created only on protected refs (the `rules:` above)
# -- a merge-request or unprotected-branch pipeline never creates them.
# GitLab refuses to create a pipeline where a job `needs:` a job that is
# absent from it, so every edge pointing at a hardware:* job (from docs:*,
# release:validate) marks it `optional: true`. The same reasoning applies
# to any other rules-gated job another job needs -- audited case by case,
# not just for hardware: docs:publish's edge to docs:build is NOT
# optional (docs:build is unconditional). The three edges inside the
# publish chain are bare strings, not `optional: true`, and must stay
# that way. The invariant that licenses a bare string is a SUBSET
# relationship, not an identical rule: a consumer's rules must select no
# pipeline its target's rules do not, so on every pipeline where the
# consumer exists its target exists too. Each of the three edges
# satisfies it:
#
#   release:validate <- release:crates   release:validate has no `rules:`
#                                        at all, so it exists on every
#                                        pipeline. Any consumer is a
#                                        subset of that trivially.
#   release:crates   <- release:pypi     Both are `<<: *on-release-tag`
#                                        -- the same rule, which is the
#                                        degenerate case of a subset.
#   release:pypi     <- release:notes    NOT the same rule since
#                                        QUI-1096. release:notes carries
#                                        its own `$CI_COMMIT_TAG =~
#                                        /^v\d/ && $CI_COMMIT_TAG !~
#                                        /-rc/`, which is strictly
#                                        narrower than on-release-tag:
#                                        the rc tags it drops are ones
#                                        release:pypi still runs on. Safe
#                                        in this direction, and only this
#                                        direction.
#
# Two edits break it, and both look like cleanups. Narrowing
# release:pypi's rules (or release:crates') without widening the
# consumer's inverts the subset and leaves a dangling non-optional
# `needs:` -- GitLab then refuses to create the tag pipeline outright,
# i.e. a release that cannot start. Folding release:notes back onto the
# shared `*on-release-tag` anchor "for consistency" restores the
# identical rule but silently reverts the QUI-1096 rc fix, publishing an
# empty or duplicate Release page for every rc tag. See release:notes'
# own `rules:` comment before touching either.
#
# release:validate carries the
# same optional hardware:*/fixture edges as docs:*, and because
# release:crates, release:pypi and release:notes each `needs:` back to
# release:validate (or to the job before them in that chain) rather than
# to this gate directly, the tag-only publish chain reaches hardware:*
# and the two fixture jobs transitively: on a tag, everything upstream of
# release:validate has to finish -- including test:substrate's cold path
# -- before the first crate or wheel goes up.
#
# The same reasoning covers the two path-gated fixture jobs (test:wasm,
# test:substrate -- see test.yml's "Path gating" section), and the audit
# lands differently in each of the two lists below:
#
#   * In `verify-test-and-hardware` it is MANDATORY. That list is used by
#     docs:* and release:validate, both of which run on merge-request
#     pipelines -- exactly where the path gate skips. Bare strings there
#     would make MR pipeline creation fail outright.
#   * In `verify-and-test` it is DEFENSIVE, and it is the entry most likely
#     to be "simplified" back to a bare string. That list is referenced only
#     by the three hardware:* jobs, which run only on protected refs, and
#     the path gate makes both fixture jobs unconditional on every protected
#     ref -- so the edge cannot dangle today. It stays optional so that a
#     future edit narrowing the gate's escape hatch (for instance back to a
#     literal "main") degrades into a skipped job rather than into
#     "protecting release/* makes pipeline creation fail", four files away
#     from the checkbox that caused it.
#
# Coverage is not weakened by either optional edge. On protected refs and
# tags both fixture jobs always exist, so docs:publish still has a real
# edge into them, and the tag-only publish chain reaches them
# transitively through release:validate's `needs:` edge into this gate,
# rather than through the stage barrier alone. On MR and pre-MR branch
# pipelines the dropped edges affect only docs:* and release:validate,
# neither of which publishes anything.
#
# The two lists below -- the ten-entry verify+test gate and the thirteen-
# entry verify+test+hardware gate -- are shared via `!reference`, not a
# YAML anchor (anchors don't cross `include:`, see this file's top-of-file
# comment), so each gate is written once here instead of being retyped
# across hardware.yml, docs.yml, and release.yml.
.needs-gates:
    verify-and-test:
        - verify:policy
        - verify:rust
        - verify:python
        - verify:parity
        - verify:xqffi
        - test:rust
        - test:python
        - test:quip
        - job: test:wasm
          optional: true
        - job: test:substrate
          optional: true
    verify-test-and-hardware:
        - verify:policy
        - verify:rust
        - verify:python
        - verify:parity
        - verify:xqffi
        - test:rust
        - test:python
        - test:quip
        - job: test:wasm
          optional: true
        - job: test:substrate
          optional: true
        - job: hardware:cuda
          optional: true
        - job: hardware:qpu
          optional: true
        - job: hardware:metal
          optional: true
