; step_limit_boundary: three instructions under a budget of two.
;
; Same program as step_limit_exact, one step short: the third instruction
; is reached with the budget spent, so both implementations raise.

PUSH 1
PUSH 2
ADD
