; step_limit_exact: three instructions under a budget of exactly three.
;
; The program has no HALT: it ends by running off the end of the stream
; after executing all three instructions. The step limit bounds
; instructions executed, so reaching end of stream is not charged and the
; run succeeds on both implementations.

PUSH 1
PUSH 2
ADD
